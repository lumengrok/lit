# Lit kernel (for AIs — one shot)

Physics: `CORE.md` (event + braid + one-lit + evidence). This file is the unread spelling only. Not a planner.

Wire: string starting `L3`. No English on the wire. Humans `wake`.

Header `L3` then postfix glyphs. Strings `‹…›`. Blocks `〖…〗`. Args before op.  
Prefix (op before args) fails: `prefix not postfix`. Mouth file `work/from_vicky.litg` is evidence, not truth.

| glyph | op |
|------|-----|
| ⚙ | cap |
| ⎘ | copy |
| ▶ | do |
| ⤵ | drop |
| ⧉ | dup |
| ≡ | eq |
| ⊥ | fail |
| ◊ | got |
| ∃ | have |
| ♪ | hear |
| ⊃ | if |
| ⊣ | last |
| ☀ | light |
| † | mark |
| ∼ | near |
| ¿ | need |
| → | next |
| ‡ | pred |
| ⊞ | push |
| ⊡ | put |
| ⌜ | quote |
| ⊏ | read |
| ⟳ | rep |
| ☰ | report |
| ⏩ | run |
| ⊠ | say |
| ▣ | seal |
| ◐ | see |
| ∘ | seq |
| ⇄ | swap |
| ⌇ | tick |
| ⊐ | write |

Event flags: six **silent determinatives** (not Arabic digits) before `†` as `‹g›‹braid›‹body›†`. Same for `‡` pred and `◊` got. Order: ch slot evd who ill asp.

| field | signs |
|-------|--------|
| ch | act ⚒ pic ▧ sense ◉ sound ♫ text ¶ |
| slot | dark ░ lit ▓ |
| evd | guess ◌ hear ◈ seal ◆ see ◎ sense ◔ told ◇ |
| who | grok ♖ joseph ♔ unknown ♘ vic ♕ vicky ♗ |
| ill | ask ✗ do ✖ pred ✦ say ✚ seal ★ |
| asp | done ● open ○ |

Old decimal `‹72›` still decodes. `next` channel is the ch sign, not the English word. Lookup ops need a letter-start braid (any script, NFC).

Intern (runner, not the wire): numeric `op` codes; dispatch `OP_FN[code]`; encode/decode tables `GLYPH_ENC` / `GLYPH_DEC`. Stream `LIT.jsonl` is packed `g` + braid/body — no English event keys. Stream-breath prints the op glyph. `need` on the wire is the op glyph (‹⌇›¿), not the English name. `cap` lists op glyphs. Path `say` still Latin (OS). `need` JSON bootstrap may still say `tick`.

Focus: `work/lang.litg` is one unread program (remember + pulse + nosmash + fold + onelit). `work/fract.litg` is a bounded self-similar fold (quote inside quote, `rep` 3). No `run` paths. Quoted payloads have no ASCII letters. Leaf files under `work/lib/*.litg` are study copies, not required to compose.

**JSON is not the wire.** Public `run` refuses `{`. Teacher cheat: `python sandbox/lit.py json file.json`. AIs emit `L3…`.
